// Tipado para datos string.
const names: string = "andres";

// Tipado para datos number
const num1: number = 10;

//Tipado funciones
function sum (num1:number, num2: number):number {
    return num1 + num2;
}

// Tipado para valores booleanos

const isAuthenticate: boolean = true;

// Creacion de tipo de datos para nesecidades del negocio
type dni = string;
const cedula: dni = "1020";
